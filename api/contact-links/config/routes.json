{
  "routes": [
    {
      "method": "GET",
      "path": "/contact-links",
      "handler": "contact-links.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contact-links/count",
      "handler": "contact-links.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contact-links/:id",
      "handler": "contact-links.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/contact-links",
      "handler": "contact-links.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/contact-links/:id",
      "handler": "contact-links.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/contact-links/:id",
      "handler": "contact-links.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
